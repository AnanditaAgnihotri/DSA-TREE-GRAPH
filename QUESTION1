#include<stdio.h>
#include <stdlib.h>

typedef struct btree{
 struct btree *lc;
 int data;
 struct btree *rc;
 }*node;
 node insert(node bt,int num);
 void inorder(node bt);
 node locate(node bt,int num,node parent,node x,int v);
 node del(node bt,int num);
 int main()
{
    int n,xin,xd,a[50];
    node bt=NULL;
    scanf("%d",&n);
    for(int i=0;i<n;i++){
        scanf("%d",&a[i]);
    }
    for(int i=0;i<n;i++){
        bt=insert(bt,a[i]);
    }
    inorder(bt);
    scanf("%d",&xin);
    bt=insert(bt,xin);
    inorder(bt);
    scanf("%d",&xd);
    bt=del(bt,xd);
    inorder(bt);
}
node insert(node bt,int num){
    if(bt==NULL){
        bt=(node)malloc(sizeof(struct btree));
        bt->data=num;
        bt->lc=NULL;
        bt->rc=NULL;
    }
    else if(num<bt->data){
        bt->lc=insert(bt->lc,num);
    }
    else{
        bt->rc=insert(bt->rc,num);
    }
    return bt;
}
node del(node bt,int num){
    node x,parent,xsucc;
    int c=0;
    if(bt!=NULL){
        locate(bt,num,parent,x,c);
        if(c==1){
            if(x->lc!=NULL & x->rc!=NULL){
                xsucc=x->rc;
                parent=x;
                while(xsucc->lc!=NULL){
                        parent=xsucc;
                    xsucc=xsucc->lc;
                }
                x->data=xsucc->data;
                x=xsucc;
            }
            //for no children root
            if(x->lc==NULL && x->rc==NULL){
            if(num==parent->rc){
                parent->rc==NULL;
            }
            else
                parent->lc==NULL;
            free(x);
            return;
        }
        else if(x->lc==NULL && x->rc!=NULL){
                if(parent->lc==num){

                }
            parent->rc=x->rc;
        }
    }
}
node locate(node bt,int num,node parent,node x,int v){
    node q=bt;
    while(q!=NULL){
    if(q->data==num){
        x=q;
        v=1;
        return v;
    }
      parent=q;
    if(num<q->data){
        q=q->lc;
    }
    else{
        q=q->rc;
    }}}
void inorder(node bt) {
    //if (bt == NULL)
    inorder(bt->lc);
    printf("%d ", bt->data);
    inorder(bt->rc);
}
